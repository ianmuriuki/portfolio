module.exports = {
    images: {
      domains: ['yourdomain.com'], // if using external images
    },
  };
// This is a Next.js configuration file that specifies the domains from which images can be loaded.
// This is useful for optimizing images in your application.  